"""
URL configuration for {{ app_name }} app.

Generated by CoreX - Django Scaffolding Framework
"""

from django.urls import path
from . import views

app_name = '{{ app_name }}'

urlpatterns = [
    # List view
    path('', views.ItemListView.as_view(), name='item_list'),
    
    # Detail view
    path('<int:pk>/', views.ItemDetailView.as_view(), name='item_detail'),
    
    # Create view
    path('create/', views.ItemCreateView.as_view(), name='item_create'),
    
    # Update view
    path('<int:pk>/edit/', views.ItemUpdateView.as_view(), name='item_update'),
    
    # Delete view
    path('<int:pk>/delete/', views.ItemDeleteView.as_view(), name='item_delete'),
    
    # Function-based views (alternative)
    path('list/', views.item_list, name='item_list_func'),
    path('<int:pk>/detail/', views.item_detail, name='item_detail_func'),
]
