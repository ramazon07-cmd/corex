# {{ project_name }}

Generated by CoreX - Django Scaffolding Framework.

## Getting Started

1. Create environment variables

```bash
cp .env .env.local  # edit values as needed
```

2. Install dependencies

- With Poetry (recommended):
```bash
poetry install --no-root
```

- Or pip:
```bash
pip install -r requirements.txt
```

3. Setup database and run migrations
```bash
python manage.py migrate
python manage.py createsuperuser
```

4. Run the server
```bash
python manage.py runserver
```

## Features

- Django {{ project_name }} base setup
- {% if api %}DRF + JWT ready{% else %}Django ready{% endif %}
- UI: {{ ui }}
- Database: {{ database }}
- Docker files provided

## Scripts

- Tailwind (if enabled):
```bash
npm install
npm run build
```

## Deploy

Use `docker-compose.prod.yml` for production or your preferred platform.

